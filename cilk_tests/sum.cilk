#include <cilk.h>
#include <iostream>

int sum = 0;
void f(int arg)
{
	sum += arg;
}

int cilk_main()
{
	cilk_spawn f(1);
	cilk_spawn f(2);
	cilk_sync;
	std::cout << "Sum is " << sum << std::endl;
}

